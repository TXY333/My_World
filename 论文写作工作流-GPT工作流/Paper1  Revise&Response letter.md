


## Reviewer1：
#### ~~Weaknesses~~  
~~一、 输入表征的局限性。作者采用幅度–相位解耦策略：输入的虚部设为 0（Im=0），实部取为强度的平方根。该处理是否遵循既有工作？尽管具有工程可行性，但并不能准确反映传播光场的物理本质。这等价于将复值系统的输入设为实值，可能在首次光学卷积发生之前就限制了复值处理的潜力。~~
 
   ~~**修改策略（必须在原文加入）：**不要辩解说这样就够了。要承认这是一个限制，但引用前人文献说明这是目前光计算的主流处理方式（Reference previous works using amplitude encoding）。同时强调，虽然输入是实数，但经过第一个透镜（傅里叶变换）后，频谱已经是复数分布，网络立刻就开始进行复数运算了。~~
- ~~**应对：** **不用补实验**，但需在 Method 部分加强解释，引用文献支持这种“Amplitude-only input”的合理性。(加入参考文献解释这种方法的合理性，并且讨论复值计算的实际发生位置在哪里)~~
~~==难点：如何做到说服我们必须采用这种设置是最符合物理规律的，同时符合本文的复值计算主题==~~


## 二、 (Lacks comparison with SOTA):** 缺少与其他光学神经网络架构的对比。

- **分析：** 确实只对比了 OP-RVNN（自建基线）。
    
- **修改策略（强烈建议补充）：** 不需要真的去搭一套别人的系统（太慢），但需要在“Introduction”或“Discussion”部分添加一个**对比表格**或段落。对比你的 OP-CVNN 与 $D^2NN$ (Lin et al., Science), ONC (Nature Comm.), Fourier 4F systems (Chang et al., Scientific Reports)。对比维度：参数量、是否复数、是否支持相位编码。
    
- **应对：** **文献调研+添加对比讨论**。
  ==难点：就是因为比不过所以才没加，那么比不过怎么解释？==

三、**(Imbalanced Accuracy/Efficiency & Marginal gains):** 审稿人指出全电网络精度更高，且你的 FLOPs 减少得不多（只少10%）。
- **分析：** 非常尖锐但客观。光学层只换了第一层，当然省不了多少 FLOPs。
    
- **修改策略（据理力争，但态度谦和）：**
    
    1. **关于精度：** 承认全电网络精度高，但指出光学计算的核心优势是**高通量、低延迟**和**能效**，且在MSTAR数据集上证明了复数建模在特定任务下的潜力。
        
    2. **关于效率：** 强调 FLOPs 不是唯一的指标。重点在于 **"Zero-energy consumption for the convolution operation itself"**（卷积本身不耗电，只有外设耗电）。如果可能，引用一些关于“光计算能效比（TOPS/W）”的文献来佐证潜力。
        
- **应对：** **修改 Discussion 部分**，坦诚精度折衷，强调“First-layer”光学化是迈向全光网络的一步，并强调由于卷积核通常很大，光学在大尺寸卷积上的$O(1)$复杂度优势。
   ==难点：按照这个思路来分析的话，难点似乎不是很大

~~四、**没有批判性地解释为什么用了光学卷积会造成性能衰减，而且光学卷积带来的性能提升也不是很大。**~~
~~. 准确率与效率权衡不充分。表 1 显示，纯电子模型（CVNN 与 RVNN）在准确率上仍优于光电混合模型（例如：MSTAR 仿真中，电子 CVNN 为 98.84%，而光学 OP-CVNN 为 91.6%）。作者虽强调效率优势（光学层零参数），但准确率损失较大，需要更批判性地讨论。作者应澄清该性能下降是否源于第一层使用固定、被动相位掩模相较于全可学习电子权重所带来的内在限制。论文声称“高建模能力”，但实验结果显示相较电子基线存在明显表征能力折损。~~
	==难点：不难解释==

五、**缺乏数据集，这是我埋的坑，MNIST和FASHION-MNIST太简单**
回复思路：用审稿人2的意见回复这个意见：就是SLM1本质是幅度型的，那么MSTAR本质上也属于是刚复杂的数据集，倒是验证了泛化性。
虽然 MSTAR 输入的也是强度，但 SAR 图像的强度图包含了独特的**相干斑噪声（Speckle noise）**和**物理散射特性**（通常服从瑞利分布）。复数网络（OP-CVNN）的优势在于，它的**内部**复数权重和非线性激活函数能够比实数网络更好地拟合这种复杂的物理统计分布。
==难点：应该不需要补充实验==


## Reviewer2：

~~一、**(CRITICAL: Input Encoding for MSTAR):** **这是最关键的一点。** 审稿人问：MSTAR 是 SAR 数据（复数），你输入进系统的时候，是真的输入了 (Real+Imaginary)，还是只输入了强度图（丢弃了相位）？~~

- ~~**分析：** 你的原文 Methods 写的是 $Im=0, Re=\sqrt{I}$。如果你用的 SLM1 是振幅型的（通常是），那你肯定丢弃了 SAR 的原始相位。如果这时候你还声称“Naturally compatible”，就是逻辑漏洞。~~
    
- ~~**修改策略（必须诚实+调整说法）：**~~
    
    1. ~~**承认：** 诚实回答在物理实验中，由于 SLM1 限制，只使用了 Magnitude 信息。~~
        
    2. ~~**补救（据理力争）：** 解释即使只输入了 Magnitude，SAR 图像的纹理特征（斑点噪声等）仍然包含丰富的物理属性。OP-CVNN 的优势在于**网络内部**采用了复数处理机制，能够像希尔伯特变换一样，从实部输入中“恢复”或“推断”出潜在的复数特征关系，比实数网络更能捕捉这些复杂的非线性关系。~~
        
    3. ~~**修改原文：** 删掉或修改宣称“使用了 SAR 原始相位”的句子，改为“利用复数网络处理 SAR 图像独特的纹理/结构信息”。~~
        
- ~~**应对：** **重大文本修改（Clarification）**。不要撒谎，否则会被拒稿。~~
==难点：承认错误，重新修改==



~~二、准直与对准问题（==简单：大论文第三章已经详细撰写，建议放在附录）==~~
~~光学对准与鲁棒性。文中使用数据增强（旋转/平移）模拟光学失准，这是合理的。但在物理实验中，作者如何处理“缩放（Scaling）”问题？SLM 的物理像素尺寸与相机传感器像素需要精确匹配，4F 系统才能作为严格的 1:1 相关器工作。建议补充像素匹配或标定流程的简述以增强严谨性。~~


~~三、误差分析（==简单：大论文第三章已经撰写，可以结合误差分析这一点联合解决Reviewer1_3==）~~
~~仿真与实验差距。表 1 中仿真到物理实验的性能下降较明显（例如 MSTAR：91.6% → 89.5%）。尽管退化可以预期，但建议更具体讨论误差来源：是对准误差、SLM 非线性还是探测器噪声？“消融实验”有所涉及，但若进一步讨论未来如何缓解这些硬件限制，将增强论文力度。~~

四、 基线对比。与 OP-RVNN 的对比在参数匹配意义上是公平的。但是否与其他光学编码方案对比过（例如标准的相位-only 相关方案，而不采用实/虚分离策略）？哪怕是简要说明或补充对比，也能更好地论证该分离策略的优越性。

~~写作与笔误。需更正以下疏漏：摘要 “TThe system intrinsically...” 应为 “The system...”；~~图 1 与图 4：请确保图中文字清晰可读；图 1(b) 模块内文字过小
