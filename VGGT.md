输入：二维图像(pair?单帧？多帧？)
问题：如何用多张二位照片推回三位里的形状
思路：同一个三维点在不同的相机里会落到不同像素上；那么只要知道相机参数并找准这些“对应像素”。就能把那条空间涉嫌交汇出三维点(称为三角化)
==相机参数== ：内参数K 外参数：描述相机在世界的姿态
外参数：负责描述相机的位置 
内参数：负责描述相机如何将光转变为像素
流程：
1.统一坐标系
2.相机标定
3.特征匹配：即如何在多张图中找到同一个三维点的坐标？多张图上提局部特征并两两匹配，用 RANSAC 过滤错配，得到“同一三维点在不同图像上的对应像素
4.两帧相对位姿
5.初始化三角化




